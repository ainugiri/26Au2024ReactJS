import React, { useState } from 'react';   // Import the useState Hook

function Result(props) {
    return(
        <div>
            <h2>Result</h2>
            
        </div>
    );
} 
export default Result;   // Export the component for use in other files